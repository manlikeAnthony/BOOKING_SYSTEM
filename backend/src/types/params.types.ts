/* forgive me for this file, I just want to have all the params types in one place 
 I know it's not the best practice but it will save me a lot of time and I don't want to create a separate file for each module
 I will refactor this file later when I have more time
 or maybe I will just leave it like this, who knows
 so please forgive me future me, I promise I will do better next time
 or not, who knows
 you i guess , or me how does this work anyway, I am confused now, I will just stop writing this comment before it gets too long and I regret it later
*/
export type HotelParams = {
    roomId: string;
    hotelId: string;
    userId: string;
    guestId: string;
    bookingId: string;
    paymentId: string;
    memberId: string;
    requestId: string;
};